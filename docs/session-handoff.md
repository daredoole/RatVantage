# Session handoff

**Token discipline:** read **[AGENTS.md](../AGENTS.md)** for crate map, build/test/lint, safety rules, and PR workflow. This file stays **short** (next tasks + prompt + safety). Long completed-slice log, full implemented inventory, and extended CLI list live in **[session-handoff-archive.md](session-handoff-archive.md)** — open only when you need that depth.

## Repo snapshot

- Repository: `https://github.com/daredoole/RatVantage` (private for now).
- Branch: `main`.
- Before editing: `git status --short --branch` and `git log --oneline -5`.
- Toolchain: `rust-toolchain.toml`; GTK needs a recent stable rustc (see toolchain file).

## Milestone (compact)

Pre-alpha: Rust workspace builds with CI green; polkit-gated daemon + GTK + StatusNotifier tray. Reversible writes now cover platform profile, battery charge type, ylogo LED, ideapad toggles (`fn_lock`, `camera_power`, `usb_charging`, `fan_mode`), firmware PPT limits, CPU governor, CPU EPP, CPU boost, conservation mode, AMD GPU DPM force level, and EnvyControl GPU mode execution behind explicit `--enable-*-write` flags. GTK fan-mode UI labels `fan_mode` as `Auto (0)` / `Full speed (1)` and keeps it separate from fan curves. GTK AMD GPU DPM apply is disabled until an operator confirms the display/GPU stability risk. Experimental all-core Curve Optimizer writes use daemon-only RyzenAdj execution behind `--enable-curve-optimizer-write`; GTK hides those controls behind an explicit Advanced CPU Tuning switch and includes a reset-to-zero button through the same daemon path. Ryzen backend status is read-only through daemon/CLI/diagnostics/GTK and includes RyzenAdj detection plus a `ryzen_smu` setup assistant that only displays commands/notes. The path remains write-only until a `ryzen_smu` read-back provider exists. Fan curves remain plan/preview only until a live writable curve surface exists. GTK UI: **sidebar nav layout** matching design hero — fixed 196px sidebar with RVLogo (Cairo-drawn ember octagon), 9 nav buttons driving adw::ViewStack (no ViewSwitcher); risk badge pills with Adwaita CSS variables; `style.css` loaded via CssProvider at startup. All write-button handlers use `make_client()` (respects dev private bus); action buttons styled with `suggested-action`/`pill` CSS. **9-tab dashboard** (Overview, Power, Battery, GPU, Fans, Devices, Automations, Settings, Diagnostics). Daemon-owned hardware profiles persist supported actions, including battery charge type for fast-charge profiles, return validator-backed dry-run previews, execute manual apply via `ApplyHardwareProfile`, map supported triggers to profiles, and record the last per-action apply run for CLI/GTK/tray diagnostics; profiles that set battery charge type and `conservation_mode` together are rejected because those firmware behaviors overlap. Automation rules persist in daemon state; `fast_charge_until_threshold` evaluates AC-online + battery capacity, previews the selected profile, test-runs through normal hardware-profile apply, and can run automatically when the daemon starts with `--enable-automation-observer`. GTK exposes saved-rule last-run history plus a real editor for enabled state, threshold, profile pickers, AC requirement, cooldown, save, test-run, preview, and delete. The automation observer polls once per minute, records last runs, and suppresses repeated same-profile applies during each rule's cooldown. Current persistence covers automation rules/last-runs, hardware profiles, hardware profile trigger mappings, hardware profile last-apply state, GPU pending state, Curve Optimizer last-write state, fan preset mapping/reapply policy, and last-known-good fan curves. Workspace tests and clippy are green. Live execute evidence: [live-validation-evidence-runbook.md](live-validation-evidence-runbook.md), [live-write-validation.md](live-write-validation.md).

## Next tasks

1. Use [vantage-toolkit-parity-test-first-plan.md](vantage-toolkit-parity-test-first-plan.md) for Vantage/Legion Toolkit parity slices. Fn+Q/platform-profile behavior is already covered. Keyboard RGB now has a common capability/request schema, validators, gated `SetKeyboardRgb` contract, read-only `PlanKeyboardRgbWrite` D-Bus/client/CLI planning surface, SDK-backed `--set-keyboard-rgb` fallback when OpenRGB SDK `backend_ready=true`, fixture-backed `KeyboardRgbCapability` metadata, fake backend apply/read-back mismatch/rollback tests, read-only HID candidate probing with report-descriptor byte count/report ID/shape evidence when sysfs exposes it, `scripts/capture-keyboard-rgb-evidence.sh` for read-only descriptor hash/hex bundles plus protocol signatures/matrix rows and operator-observed hotkey/effect notes, `scripts/compare-keyboard-rgb-evidence.sh` for cross-bundle protocol clustering, `scripts/check-keyboard-rgb-openrgb.sh` for standalone OpenRGB readiness evidence including user/group membership, device-node owner/group/access details, missing access, and setup recommendation, `scripts/setup-keyboard-rgb-openrgb-access.sh` / `/usr/local/sbin/ratvantage-setup-keyboard-rgb-openrgb-access` for explicit one-time i2c group/module/udev setup with GUI copy-command guidance when access is missing, `PlanOpenRgbKeyboardRgbBridge` / `--plan-openrgb-keyboard-rgb` dry-run OpenRGB command previews, GTK Appearance staged RGB controls for effect, per-zone `#RRGGBB` colors, brightness, speed, request JSON copy, read-only daemon/OpenRGB plan preview, inline `Check status` that captures OpenRGB readiness then runs the readiness-aware bridge evidence helper, non-mutating `Capture dry-run`, and read-only `Review execute`, `scripts/capture-keyboard-rgb-openrgb-bridge-evidence.sh` plus user-installed `ratvantage-capture-keyboard-rgb-openrgb-bridge-evidence` for dry-run-by-default OpenRGB save/apply/mode-read-back/restore/profile-color-byte evidence bundles, `scripts/status-keyboard-rgb-openrgb-bridge-evidence.sh` plus user-installed `ratvantage-keyboard-rgb-openrgb-bridge-status` for OpenRGB readiness preflight, dry-run/execute bundle status, and next-action summaries, `scripts/review-keyboard-rgb-openrgb-bridge-evidence.sh --require-promotable <bundle>` plus user-installed `ratvantage-review-keyboard-rgb-openrgb-bridge-evidence` for promotion review, `keyboard_rgb_status` and `keyboard_rgb_openrgb` overview readiness output, and tray/menu/dashboard RGB readiness surfaces with copyable OpenRGB evidence commands. Live dogfood evidence after `scripts/update-dev-install.sh --daemon` and bundle `target/validation/keyboard-rgb-live-readonly`: `hidraw1 048d:c103 bytes=156 sha256=e086fca03bf4 reports=1,2,3,90 shapes=90/feature:16B + small input/output reports`; `hidraw2 048d:c985 bytes=179 sha256=090f0dd411d6 reports=1,5,7,90,204 shapes=90/feature:16B,7/feature:191B,204/feature:32B + input/output reports`; `hidraw3 048d:c985 bytes=25 sha256=9d32406ef8a2 reports=none shapes=feature:64B,output:64B`. Current operator-observed firmware behavior is `Fn+Space` cycling RGB and visible `breathing`; built-in OpenRGB readiness probing detects `Lenovo 5 2023` / `Lenovo 4-Zone device` with `Direct`, `Breathing`, `Rainbow Wave`, and `Spectrum Cycle`, and current user device access is via ACLs on `/dev/i2c-*`/selected `/dev/hidraw*`, not `i2c` group membership. Real RatVantage RGB execution now works through the daemon OpenRGB SDK fallback: live `--set-keyboard-rgb` applied `Breathing` with all four zones `#333333`, method `SetOpenRgbKeyboardRgbSdk`, read-back `active_mode=Breathing;colors=left_center:#333333,left_side:#333333,right_center:#333333,right_side:#333333`. The cleaned live OpenRGB CLI execute bundle at `target/validation/keyboard-rgb-openrgb-bridge-execute` remains negative, so direct OpenRGB CLI promotion stays blocked. Custom thermal dry-run planning now annotates firmware PPT, fan preset, and restore-auto-fan plans with missing/unavailable/satisfied/required `custom` platform-profile prerequisites plus previous-profile rollback guidance, `--plan-prepare-custom-thermal` exposes the plan-only switch/read-back/rollback prerequisite, and `--plan-custom-thermal-firmware-attribute`, `--plan-custom-thermal-firmware-ppt-preset`, `--plan-custom-thermal-fan-preset`, and `--plan-custom-thermal-restore-auto-fan` expose sequence previews with reverse rollback order while normal `SetPlatformProfile custom` stays blocked; checked-in fixtures cover the unavailable case, while live dogfood shows `platform_profile_choices=low-power balanced performance max-power custom`, current `low-power`, and PPT planning requires switching to `custom` before applying. Firmware PPT metadata now includes optional `attribute_type`/`default_value`, `--plan-firmware-attribute-reset <attribute_id>` plans a validated reset when default metadata exists, read-only PPT presets cover `conservative`, `balanced-custom`, `performance-custom`, and `reset-defaults` by expanding to PL1/PL2/PL3 plans after custom thermal preparation, and GTK Profiles exposes preset Preview/Copy Plan rows before any firmware write is attempted. Track D profile-change integration now persists `last_observed_platform_profile`; the opt-in automation observer baselines the current firmware profile, detects external changes, and applies a configured `platform_profile_changed` hardware-profile trigger through the existing profile-apply policy/validator/read-back path. Other high-value targets are safer GPU switching research and RatVantage-only automation/evidence features. Follow the plan's test lanes before adding execution paths.
1. Latest dev updater slice: `scripts/dev-daemon-args.sh` is now the single repo-owned broad dev daemon flag list. `scripts/install-dev-passwordless-updater.sh` installs a root helper marked `repo-driven-daemon-args-v1` that reads that file at daemon refresh time, so future daemon flag additions should not require editing/reinstalling the root helper. `scripts/update-dev-install.sh --daemon` treats older helpers as stale and still stops non-interactively until `sudo /home/darrian/projects/RatVantage/scripts/install-dev-passwordless-updater.sh` refreshes the root-owned helper once. Regression coverage: `scripts/test-dev-daemon-args.sh` and `scripts/test-update-dev-install-passwordless.sh` are included in `scripts/ci-local.sh`.
1. Latest RGB setup slice: `scripts/setup-keyboard-rgb-openrgb-access.sh` is idempotent for users already in `i2c`; daemon/client/D-Bus now expose `PlanOpenRgbAccessSetup` and gated `SetupOpenRgbAccess` behind `org.ratvantage.LegionControl1.setup-openrgb-access` plus `--enable-openrgb-access-setup`, and GTK Appearance shows a `Set up` button with feedback plus a copyable fallback command when OpenRGB access is missing. The setup row now appears for incomplete `i2c-dev`, `i2c` rw, or `hidraw` rw checks and names the missing access. `scripts/install-user-session.sh` still installs `ratvantage-check-keyboard-rgb-openrgb`, a user-local setup helper, and a `ratvantage-setup-keyboard-rgb-openrgb-access` wrapper that uses the root-owned helper with `sudo -n` when the dev passwordless rule is current before falling back to an interactive sudo command. Current local machine state after the one-time sudo refresh: `/usr/local/sbin/ratvantage-dev-update-daemon` has the `repo-driven-daemon-args-v1` marker, `scripts/update-dev-install.sh --daemon` runs non-interactively, `legion-control-daemon.service` is active, OpenRGB access checks pass (`i2c_rw=true`, `hidraw_rw=true`), and overview reports OpenRGB SDK `backend_ready=true`; the current login still reports `user_in_i2c_group=false`, so group membership may require a new session even though ACL/device rw checks already pass.
1. Latest compatibility/package slice: `scripts/capture-compatibility-bundle.sh` captures read-only overview, diagnostics, automation diagnostics, reset diagnostics, probe JSON, OpenRGB readiness, OpenRGB bridge evidence status, OpenRGB SDK read-back evidence, and GPU mux/session-restart `mux-only` evidence into one support directory; bundle metadata now includes compact `high_value_recovery` reset summaries for Curve Optimizer, firmware PPT defaults, RGB SDK recovery, GPU pending recovery, GPU switching recovery, restore-auto-fan, and custom-thermal restore-auto-fan, `high_value_drift` summaries for hardware-profile and fan-curve drift, `high_value_gpu_switching` classification summaries for reboot/session/runtime GPU switching support, `high_value_gpu_mux` summaries for current EnvyControl mode/DRM provider count/NVIDIA PCI rows/first D3cold indicator from structured `gpu-mux/pre/mux-summary.json`, and `high_value_automation` summaries for saved profiles, triggers, automation rule count/kinds, first-rule profile intent, last rule runs, recent platform-profile changes, and recent desktop PowerProfiles changes. Bundle Markdown/PR text now surfaces GPU switching next action, first blocker/evidence, mux current mode, first D3cold indicator, hardware-profile drift status, fan-curve drift status, automation rule kinds, and the first automation rule target directly. GTK GPU and Fans pages expose copyable read-only evidence command groups for GPU switching and fan validation bundles, live curve readings, saved snapshots, and reset/restore plans. Bundles also write `compatibility-bundle-pr-body.md` for hardware report submissions. `scripts/install-user-session.sh` installs `legion-probe`, `ratvantage-capture-compatibility-bundle`, the GPU mux helper, the OpenRGB readiness/bridge/SDK helpers, and the HID descriptor capture/compare helpers. RPM packaging now has a `legion-control-helpers` subpackage required by the UI package for those same support scripts, with no setuid helper; local CI covers the metadata with fake UI/probe/OpenRGB/status commands plus `scripts/test-rgb-helper-packaging-metadata.sh`.
1. Latest automation slice: daemon-owned `ac_profile_router` rules select a stored hardware profile from AC-online/offline telemetry, daemon-owned `battery_profile_threshold` rules select one stored hardware profile from battery capacity telemetry with at/below or at/above threshold matching plus optional AC-online/offline requirements, daemon-owned `fast_charge_until_threshold` rules route below-threshold charging vs protect profiles, and daemon-owned `periodic_idle` rules reselect one stored hardware profile on observer ticks with same-profile cooldown for state correction. Mapped hardware-profile triggers now include `resume`, `platform_profile_changed`, `desktop_power_profile_changed`, and `gpu_mode_reboot_completed` through the opt-in observer; desktop PowerProfiles changes are baselined, recorded in `recent_desktop_power_profile_changes`, exposed by `legion-control-ui --recent-desktop-power-profile-changes`, included in `--automation-diagnostics`, and rendered in GTK Automations. All automation/profile applies preview through the existing evaluator, apply through `ApplyHardwareProfile`, honor observer cooldown, and render in GTK saved rules with preview/test/delete controls. `ac_profile_router` has AC/battery profile pickers plus save controls, a Create AC Router Rule form for custom AC-online/battery-power routing, and GTK Quick Template starters for `plugged_in_balanced` / `battery_saver` / `ac_router` plus `ac_cpu_performance` / `battery_cpu_efficiency` / `ac_cpu_router`; the CPU router starter adds platform profile, CPU governor, CPU EPP, and CPU boost actions through existing daemon gates. `battery_profile_threshold` has GTK editing for profile, threshold, direction, AC condition, enabled state, cooldown, save, a Create Automation Rule form for custom battery-threshold rules, and Quick Template starters for `quiet_below_30` and `battery_recovered_80`. `desktop_power_balanced_repair` is available from the Desktop Power Repair quick template and maps `desktop_power_profile_changed` to balanced platform/CPU repair actions. `periodic_balanced_repair` and `periodic_idle_correction` are available from the Periodic Idle Correction quick template for balanced platform/CPU state repair every 30 minutes. `fast_charge_until_threshold` has GTK editing plus a Create Fast Charge Rule form for custom fast-charge/protect profile routing. Focused coverage: `cargo test -p legion-control-daemon periodic_idle_automation_reapplies_profile_with_cooldown --quiet`, `cargo test -p legion-control-daemon desktop_power_profile_change_observer_applies_mapped_trigger_once --quiet`, `cargo test -p legion-control-daemon ac_profile_router_preview_includes_cpu_tuning_actions --quiet`, `xvfb-run -a cargo test -p legion-control-ui --features gtk-ui --test gtk_shell automations_page_renders_ac_profile_router_starter --quiet`, and `xvfb-run -a cargo test -p legion-control-ui --features gtk-ui --test gtk_shell automations_page_renders_ac_profile_router_rules --quiet`.
1. Latest mixed preset slice: GTK Automations has `Balanced daily mixed profile starter`, `Quiet battery mixed profile starter`, and `Performance AC mixed profile starter`. `balanced_daily_mixed` saves balanced platform, Standard charge type, CPU governor/EPP/boost, AMD GPU DPM auto, staged RGB intent, and `balanced -> balanced-daily` fan mapping. `quiet_battery_mixed` saves low-power platform, Conservation charge type, CPU efficiency settings, AMD GPU DPM low, staged low-brightness RGB intent, and `low-power -> quiet-office` fan mapping. `performance_ac_mixed` saves performance platform, Standard charge type, CPU performance settings, AMD GPU DPM auto, staged RGB intent, maps `ac_connected` to the profile, and records `performance -> performance-sustained`. Daemon preview coverage proves all three mixed profiles expand to `SetPlatformProfile`, `SetBatteryChargeType`, `SetKeyboardRgb`, `SetCpuGovernor`, `SetCpuEpp`, `SetCpuBoost`, and `SetAmdGpuDpmForceLevel` dry-run plans without enabling new execution paths. Focused coverage: `cargo test -p legion-control-daemon mixed_daily_hardware_profile_preview_combines_safe_actions_and_staged_rgb --quiet`, `cargo test -p legion-control-daemon quiet_battery_mixed_profile_preview_combines_low_power_actions_and_staged_rgb --quiet`, and `cargo test -p legion-control-daemon performance_ac_mixed_profile_preview_combines_performance_actions_and_staged_rgb --quiet`.
1. Latest profile-change history slice: the daemon now stores bounded `recent_platform_profile_changes` when the opt-in observer sees external profile changes such as Fn+Q/firmware-side changes; D-Bus exposes `GetRecentPlatformProfileChanges`, `legion-control-ui --recent-platform-profile-changes` prints the list directly, `--automation-diagnostics` plus diagnostics/support bundles include the list, GTK Automations renders recent `previous -> current` changes, and the `Fn+Q tuning repair starter` seeds a CPU-tuning profile mapped to `platform_profile_changed`.
1. Latest GPU reboot-completion automation slice: `gpu_mode_reboot_completed` is now a supported hardware-profile trigger. The opt-in automation observer checks durable `gpu_mode_pending`, compares it with the current EnvyControl probe after reboot, clears the pending state when the requested mode is active, and applies a mapped `gpu_mode_reboot_completed` profile through the existing hardware-profile policy/read-back gates. GTK Automations labels the trigger as `GPU reboot completed` and has a `GPU reboot repair starter` that seeds `gpu_reboot_completed_balanced` mapped to the trigger.
1. Latest resume reapply slice: the logind resume observer now keeps the existing fan-preset dry-run plan and also applies a mapped `resume` hardware-profile trigger through the daemon's normal hardware-profile policy/read-back gates; GTK Automations copy explains that resume triggers run automatically after logind resume, and the `Resume balanced profile starter` seeds a balanced repair profile mapped to `resume`.
1. Latest Ryzen backend setup slice: daemon detection reports RyzenAdj availability plus `ryzen_smu` module/sysfs/pm_table read-back state; `legion-control-ui --ryzen-backend-status` and `--ryzen-smu-setup` print read-only JSON; GTK Profiles shows Ryzen Backend Status; GTK Diagnostics now exposes copyable Ryzen status/setup commands and explicitly says RatVantage does not install or load modules automatically. Focused GTK coverage: `xvfb-run -a cargo test -p legion-control-ui --features gtk-ui --test gtk_shell diagnostics_page_surfaces_compatibility_bundle_command --quiet`.
1. Latest drift diagnostics slice: diagnostics now derive `hardware_profile_drift` from the last completed hardware-profile apply and current read-only probe data for comparable actions such as platform profile, battery charge type, GPU mode, CPU power, conservation mode, AMD GPU DPM, firmware attributes, and OpenRGB SDK mode/colors; diagnostics also derive read-only `fan_curve_drift` by comparing live fan curve readings from `GetLiveFanCurveReadings` against the stored last-known-good fan curve snapshot. `--diagnostics` / `--automation-diagnostics` include both reports, GTK Diagnostics shows compact hardware-profile and fan-curve drift summaries, runtime refresh notices report live fan curve drift, and StatusNotifier sends de-duplicated desktop notifications when new profile or fan-curve drift appears.
1. Latest GPU research slice: shared `GpuSwitchType` metadata classifies GPU mode switching as `unknown`, `reboot-required`, `session-restart-required`, or `runtime-mux`; EnvyControl probes report `reboot-required`, overview/tray/GPU page/dry-run plans surface the switch type/evidence notes, diagnostics now include structured `gpu_switching` provider/current-mode/execution-model/blocker/evidence/next-action data, GTK GPU shows the status/model/runtime-plan/blocker rows, tray status/tooltip/menu add a read-only `GPU switching` classification when diagnostics are meaningful, and `session-restart-required`/`runtime-mux` classifications are validation-blocked until a dedicated backend and live display recovery evidence exist. `scripts/capture-gpu-mux-evidence.sh` / `ratvantage-capture-gpu-mux-evidence` captures read-only mux/session-restart evidence (`mux-only`, `pre`, `post`, `compare`) with text reports plus `mux-summary.json` / `compare-summary.json`; `scripts/review-gpu-mux-evidence.sh` / `ratvantage-review-gpu-mux-evidence` validates phase summaries and has `--require-session-restart-confirmed` for strict pre/post compare proof before any session-restart switch planning is considered. Both helpers are installed with user/RPM helper scripts and are included in GTK GPU copyable evidence commands; live `mux-only` capture on this machine succeeds at `target/validation/gpu-mux-evidence/pre`.
1. Latest RGB SDK/status evidence slice: `scripts/capture-keyboard-rgb-openrgb-sdk-evidence.sh` is a read-only OpenRGB SDK probe that starts or connects to an SDK server, negotiates protocol with the client version, waits for controller enumeration after the SDK port opens, skips async device-list packets, parses controller version/zone segment/LED value data, and records active mode, modes, zones, LEDs, and colors into JSON/Markdown without writing RGB. It is installed as `ratvantage-capture-keyboard-rgb-openrgb-sdk-evidence`, included in compatibility bundles, exposed in GTK Appearance as `OpenRGB SDK read-back evidence` / `Check SDK`, and covered by fake immediate/delayed SDK server tests. `scripts/capture-keyboard-rgb-openrgb-bridge-evidence.sh` captures SDK before/after/restored snapshots around execute mode, accepts SDK-backed color evidence in its result, records whether OpenRGB CLI used `--noautoconnect`, and can test device selection by detected name or index; `scripts/review-keyboard-rgb-openrgb-bridge-evidence.sh` accepts either profile-byte or SDK color proof, while requiring SDK colors to restore to the before snapshot when SDK color proof is used. OpenRGB CLI apply variants remain negative by SDK read-back: name/default, lowercase `breathing`, `--allow-autoconnect`, `--device-selector index`, and `Direct` colors all leave `after_mode=Direct` and colors `000000,000000,000000,000000`. The operator-triggered `scripts/capture-keyboard-rgb-openrgb-sdk-write-evidence.sh` now sends `RGBCONTROLLER_UPDATEMODE` plus `RGBCONTROLLER_UPDATELEDS` through the SDK only with `--execute`, reads back requested mode/colors, restores the before mode/colors, and records no sysfs/hidraw/i2c/WMI/EC writes; it is installed as `ratvantage-capture-keyboard-rgb-openrgb-sdk-write-evidence`, listed in GTK Appearance as a copyable `OpenRGB SDK write evidence` command, covered by a fake SDK server test, and included in local CI. Current live `target/validation/keyboard-rgb-openrgb-sdk-write` evidence is positive: requested `Breathing`, before `Direct`, after `Breathing`, restored `Direct`, colors `FF0000,00FF00,0000FF,FFFFFF`, `mode_readback_matches=true`, `color_readback_matches=true`, `restore_color_matches=true`, `restore_mode_matches=true`, `sdk_write_ready_evidence=true`, blockers `0`. `scripts/status-keyboard-rgb-openrgb-bridge-evidence.sh` includes `sdk_write` in text/JSON status and reports `promotable=true` for that mode/color evidence. Daemon planning now exposes read-only `PlanOpenRgbKeyboardRgbSdkWrite`, OpenRGB-only hardware-profile RGB preview falls back to `SetOpenRgbKeyboardRgbSdk` with SDK packet/read-back/restore instructions instead of the negative CLI bridge, `SetKeyboardRgb` falls back to the SDK writer when native keyboard RGB is absent, and `legion-control-ui --plan-openrgb-keyboard-rgb-sdk <json>` exposes that read-only SDK plan from the CLI. The daemon now has `OpenRgbKeyboardRgbSdkWriter`, `CommandOpenRgbKeyboardRgbSdkWriter`, hardware-profile and direct `SetKeyboardRgb` SDK fallback apply/read-back mismatch rollback/rollback-failure tests, a fake external-helper process-boundary test, opt-in `--enable-keyboard-rgb-write`, opt-in `--openrgb-sdk-helper <path>`, and dry-run output for `openrgb_sdk_helper`. `scripts/openrgb-keyboard-rgb-sdk-helper.sh` is the real connect-only helper (`snapshot`, `write`, `restore`) installed as `ratvantage-openrgb-keyboard-rgb-sdk-helper`; it connects to an already-running OpenRGB SDK server via `RATVANTAGE_OPENRGB_SDK_HOST/PORT` (default `127.0.0.1:6742`) and is covered by a fake SDK server test. `KeyboardRgbOpenRgbStatus` now exposes `sdk_helper_installed`, `sdk_server_running`, `sdk_snapshot_supported`, `sdk_active_mode`, `sdk_color_zones`, `sdk_colors`, and evidence-backed `backend_ready`; live `legion-probe` reports `backend_ready=true`, `sdk_active_mode=Direct`, and zones `left_center,left_side,right_center,right_side` against the running user-session SDK server. `legion-control-ui --reset-diagnostics` now includes a read-only `keyboard_rgb_sdk_recovery` entry that builds a dry-run plan for restoring the current SDK mode/colors when snapshot colors are available, and hardware-profile drift detection compares OpenRGB SDK RGB read-back (`active_mode=...;colors=...`) against current SDK snapshots. Dev daemon args enable `SetKeyboardRgb` and pass the user-installed SDK helper path; daemon-side capability refresh annotates OpenRGB SDK backend readiness from the configured writer, GTK `Start server` refreshes capabilities, RPM helper packaging covers the SDK/readiness/evidence/compatibility scripts, live system-daemon dogfood applied `Breathing` / `#333333` through `SetOpenRgbKeyboardRgbSdk`, and the tray menu now exposes guarded RGB presets (`direct-dim`, `breathing-dim`, `rainbow-wave`, `spectrum-cycle`) when backend readiness is proven; live `legion-control-tray --action set_keyboard_rgb:breathing-dim` applied and read back `Breathing` / four-zone `#333333`.
1. Latest RGB SDK server helper slice: `scripts/openrgb-sdk-server-session.sh` is installed as `ratvantage-openrgb-sdk-server`, supports `status/start/stop/restart`, runs OpenRGB as the desktop user, writes PID/logs under user-owned runtime/cache paths, checks for a stable listening port without consuming an SDK connection, now requires a two-second stability window by default, and is covered by `scripts/test-openrgb-sdk-server-session.sh`. GTK Appearance includes an `OpenRGB SDK server` row with `Start server` and `Copy server` controls. Live dogfood now starts a durable default-port SDK server on this machine, and `ratvantage-openrgb-keyboard-rgb-sdk-helper snapshot openrgb-sdk:/usr/bin/openrgb` succeeds against it.
1. Continue the automation engine described in [automation-engine-plan.md](automation-engine-plan.md): advanced CPU actions already execute through hardware profiles, GTK Automations surfaces profile-level governor/EPP/boost/CO actions with read-back/write-only warnings when rules reference those profiles, and GTK Power now has an Advanced CPU Profile Builder for saving governor/EPP/boost/CO profiles without applying them. Battery charge type is a real hardware-profile action; same-profile conflicts with conservation mode are rejected; keyboard RGB validates/previews through native `SetKeyboardRgb` when a native backend is promoted, through `SetOpenRgbKeyboardRgbSdk` when the OpenRGB SDK backend is ready, or through the dry-run OpenRGB bridge when only CLI evidence exists; GTK has starters for `fast_charge` / `battery_protect`, balanced daily / quiet battery / performance AC mixed profile composition, AC profile routing, AC CPU performance/efficiency routing, `quiet_on_battery` with AC-unplugged trigger, `quiet_below_30` battery threshold rule, and `low-power -> quiet-office` fan-preset app-state mapping, custom AC-router, battery-threshold, and fast-charge rule creation, `battery_recovered_80` mapping battery 80% or higher back to `battery_recovered_balanced`, `integrated_gpu_on_battery` with reboot-gated `gpu_mode=integrated`, `resume_balanced_repair` mapped to `resume`, `fnq_balanced_cpu_repair` mapped to `platform_profile_changed`, `gpu_reboot_completed_balanced` mapped to `gpu_mode_reboot_completed`, `rgb_breathing_blue` for RGB profile setup, and `co_experimental_minus10` for preview-only all-core CO setup; saved profile rows now show compact action summaries such as `gpu=integrated`, `rgb=Breathing #333333`, and `CO=-10`; `legion-control-ui --automation-diagnostics` prints one read-only support snapshot with profiles, trigger mappings, rules, recent platform-profile changes, last automation runs, and last hardware-profile apply, and GTK Diagnostics now exposes a copyable automation diagnostics command; `legion-control-ui --reset-diagnostics` prints one read-only reset-plan snapshot with per-family plan/execute/clear/verify command strings for CO, firmware PPT reset-defaults, OpenRGB SDK RGB recovery, restore-auto-fan, custom-thermal restore-auto-fan, GPU pending recovery, and GPU switching recovery guidance, GTK Diagnostics exposes copyable reset diagnostics plus recovery command references, and compatibility bundles summarize those commands; automatic execution is opt-in with `--enable-automation-observer`.
1. Continue live write-validation on supported Legion hardware, **one control at a time**. Accepted evidence now exists for CPU boost, CPU governor, CPU EPP, conservation mode, AMD GPU DPM force level, Curve Optimizer apply/reset, EnvyControl GPU mode execution, one saved hardware profile manual apply, and one hardware-profile trigger apply. Firmware PPT writes reached the Lenovo WMI attribute files but all three returned `Device or resource busy (os error 16)`, so PPT stays gated/experimental until the busy firmware state is understood. Fan mode has negative live evidence (`1` read back as `0`, then rollback restored `0`) and should stay unpromoted unless a different validated driver path appears. The full live verifier now accepts those PPT/fan bundles only as negative execute evidence with the exact failure signatures. The deliverable audit and evidence gate are summarized in [driver-surface-read-write-gui-plan-82wm.md](driver-surface-read-write-gui-plan-82wm.md). Use the exact `control_id` table in [live-write-validation.md](live-write-validation.md); advanced controls are plan-only unless `--execute-only` matches the family. Use the documented `validation_cpu_driver` seed plus trigger mapping for reproducible profile evidence; local CI now asserts that seeded fixture previews contain `SetCpuGovernor`, `SetCpuEpp`, and `SetCpuBoost`, and the live verifier requires live metadata (`sysfs_root=/`, `target_bus_mode=system` or `custom-address`), expected dry-run method names, expected polkit actions, expected readback flags, and per-action results for those same controls for both manual-profile and trigger-profile bundles. Include the required operator checklists for fan mode thermal/fan behavior, GPU mode EnvyControl reboot/recovery evidence, and Curve Optimizer reset/stability evidence; the Curve Optimizer bundle must also include daemon `GetLastCurveOptimizerAllCore` state after apply and reset. Review with `scripts/review-write-validation-bundle.sh --require-mode execute --require-control <control_id>=pass` where reversible apply+revert is expected, and use the matrix status for negative `executed` controls. The review gate has regression coverage in `scripts/test-review-write-validation-bundle.sh`. After all bundles exist, run `scripts/verify-82wm-live-evidence.sh --root target/validation`; its required control/status/daemon-flag/output-slug matrix is [82wm-live-evidence-requirements.tsv](../data/validation/82wm-live-evidence-requirements.tsv), and every bundle directory must match `target/validation/82wm-live-<output_slug>`. Verifier regression coverage lives in `scripts/test-verify-82wm-live-evidence.sh` plus `scripts/ci-local.sh`.
2. After reinstalling/restarting the system daemon, verify `legion-control-ui --overview` reports `desktop_power_profiles=bus=system ... active=...` on Fedora/Plasma.
3. If the KDE Wayland/NVIDIA GTK black-window issue recurs, treat it as a frontend/compositor bug: keep tray/CLI validation via `scripts/run-local-session-app.sh` while isolating the renderer path.
4. Keep tray autostart disabled until GNOME-with-extension smoke exists; KDE smoke is not the blocker.
5. GTK Fans manual curve work stays **after** the kernel exposes writable `pwm*_auto_point*` files and live evidence exists; fan preset execution stays disabled until policy and evidence gates are met.
6. Keep `docs/feature-roadmap.md` / `docs/implementation-plan.md` aligned when scope changes.
7. Do not enable higher-risk hardware mutation until validators, polkit, rollback, and manual validation exist.

## When you finish a slice

- Run `./scripts/ci-local.sh`, commit, **update this file** (Next tasks / milestone if needed).
- For local dogfood updates, use `scripts/update-dev-install.sh` to rebuild/install the tray + GTK dashboard and restart the tray; add `--daemon` when the system daemon/polkit/D-Bus install also needs refreshing.
- Passwordless daemon dogfood updates are now active on this machine. `scripts/update-dev-install.sh --daemon` should use `sudo -n`, read flags from `scripts/dev-daemon-args.sh`, install `/usr/local/sbin/ratvantage-setup-keyboard-rgb-openrgb-access`, refresh user tray/dashboard binaries, and stop prompting.
- Optional audit trail: append one line under **Completed slice log** in [session-handoff-archive.md](session-handoff-archive.md).

## New session prompt

Start with:

```text
Read AGENTS.md and docs/session-handoff.md first. Open docs/session-handoff-archive.md only if you need the long completed-slice log or full command inventory. Act as orchestrator: inspect git status, pick the next roadmap slice, delegate bounded work when useful, validate with ./scripts/ci-local.sh, update docs, commit. Do not change safety constraints.
```

## Safety constraints

- No raw WMI calls.
- No raw EC writes.
- No arbitrary sysfs writer.
- No hardcoded `hwmonN`.
- No hardware writes until validators, polkit policy, rollback behavior, and manual validation exist.
