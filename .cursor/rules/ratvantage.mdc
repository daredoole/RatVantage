---
description: RatVantage — points to AGENTS.md (canonical); Cursor-only reminder
alwaysApply: true
---

# RatVantage (Cursor)

**Do not duplicate long rules here.** Read **[AGENTS.md](../../AGENTS.md)** for crate layout, `cargo` / `./scripts/ci-local.sh`, safety constraints, and PR workflow.

**Continuation:** [docs/session-handoff.md](../../docs/session-handoff.md) — short public-safe maintainer context. Keep private machine state and raw validation evidence outside tracked docs.

**Cursor reminder:** this rule loads in addition to `AGENTS.md`; keep edits minimal and match existing style.
